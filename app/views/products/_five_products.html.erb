<div class="row">
  <% items.each do |item| %>
    <div class="col-md-2">
      <p><%= link_to item.name, product_path(item) %></p>
      <p><%= item.rating %></p>
      <p><%= image_tag item.picture.url(:thumb) %></p>
      <p><%= item.price %></p>
    </div>
  <% end %>
</div>